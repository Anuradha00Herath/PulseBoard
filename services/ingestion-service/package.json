{
  "name": "ingestion-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "lint": "echo \"(lint stub) no linter configured yet\" && exit 0",
    "test": "echo \"(test stub) no tests yet\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "kafkajs": "^2.2.4",
    "ioredis": "^5.4.1"
  }
}
